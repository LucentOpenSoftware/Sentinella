# Rust
/target/

# Test artifacts
test-corpus/
**/*.rs.bk
Cargo.lock

# Node
node_modules/
dist/

# Tauri
gui/src-tauri/target/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Build artifacts (NOT .exe — we keep sentinelld.ico and scripts)
*.dll
*.so
*.dylib
*.msi
*.deb
*.rpm
*.AppImage
*.pdb

# ClamAV databases
*.cvd
*.cld
*.cbc

# ClamAV upstream build artifacts
third_party/clamav/build/
third_party/clamav/target/
third_party/clamav/clamav-main.zip

# Out-of-tree ClamAV build
/build/

# vcpkg (large, not committed)
third_party/vcpkg/

# Runtime data (not committed — user-specific)
runtime/signatures/
runtime/logs/
runtime/state/
runtime/quarantine/
runtime/research_samples/*.exe
runtime/research_samples/*.dll
runtime/research_samples/*.zip
runtime/research_samples/*.rar
runtime/compiled/
runtime/clamav_tmp/
gui/src-tauri/runtime/state/
gui/.tmp/
runtime/config/freshclam.resolved.conf

# SQLite WAL files
*.db-wal
*.db-shm

# Release staging (generated by packaging scripts)
release/staging/
release/*.zip

# graphify output (analysis artifacts)
graphify-out/
.graphify_*
