# Rust build
/target/
Cargo.lock
**/*.rs.bk

# Node
node_modules/
dist/

# Tauri build
gui/src-tauri/target/
gui/.tmp/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Build artifacts
*.dll
*.so
*.dylib
*.msi
*.deb
*.rpm
*.AppImage
*.pdb

# ClamAV databases (downloaded at runtime)
*.cvd
*.cld
*.cbc

# ClamAV build
third_party/clamav/build/
third_party/clamav/target/
third_party/clamav/clamav-main.zip

# vcpkg (large, fetched by build script)
third_party/vcpkg/

# Out-of-tree ClamAV build
/build/

# Runtime user data (created by daemon, never committed)
runtime/cache/
runtime/clamav_tmp/
runtime/diagnostics/
runtime/logs/
runtime/quarantine/
runtime/signatures/
runtime/state/
runtime/update_staging/
runtime/research_samples/
runtime/test_runtime/
runtime/compiled/
runtime/config/freshclam.resolved.conf
runtime/freshclam-bootstrap.conf
runtime/signatures_bootstrap/

# Fuzz build artifacts
fuzz/target/

# SQLite
*.db-wal
*.db-shm

# Release outputs
release/staging/
release/*.zip

# Signing keys (NEVER commit)
keys/

# Test artifacts
test-corpus/

# graphify analysis
graphify-out/
.graphify_*
